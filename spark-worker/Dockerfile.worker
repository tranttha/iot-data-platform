FROM bitnamilegacy/spark:3.5

USER root

# Install Python dependencies from requirement.txt so UDFs can import them on workers.
COPY requirement.txt .
RUN pip install --no-cache-dir -r requirement.txt
